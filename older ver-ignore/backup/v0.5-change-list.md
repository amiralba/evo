# EVO v0.5 — Change List (triage of the GPT brainstorm)

Sorted by bucket, then by value-per-effort. Source = brainstorm question. Effort is prototype effort (S/M/L), not real-build.

## Bucket 0 — Fixes (defects, not ideas)

| # | Change | Source | Effort | Status |
|---|--------|--------|--------|--------|
| 0.1 | Cascade fix: Baz edits no longer wipe yamalar. `clearFutureWeeks` reprojects future weeks preserving patches; `setStoreFreq` rebuilds only the affected store via `rebaseStoreWeek` | Q2 "verify ours" | S | **Done (v0.4.1)** |
| 0.2 | Cache desync fix: flows that reassign `visits` (pasifleştir, havuza taşı, rut sil, revertPatch) now write back to `weekData[currentWeek]` — removed visits no longer resurrect on week navigation | found during 0.1 | S | **Done (v0.4.1)** |

## Bucket 1 — v0.5 prototype (build)

| # | Change | Source | Effort | Notes |
|---|--------|--------|--------|-------|
| 1.1 | **Override-with-reason publish gate** (our synthesis): severity tiers 🔴/🟡/🔵, but errors don't hard-block — publishing past an error requires a recorded justification (reason + who decided) | Q5 + Q13 | M | One mechanism replaces both "hard block-list" and "Decision Journal"; preserves "never block" DNA |
| 1.2 | **Conflict Center + scope-based placement**: problem list grouped by severity, click-to-jump; one status per visit card; day-header counts | Q5 | M | The triage answer for 400 stores; best IA idea in the log |
| 1.3 | **Decision Journal lite**: extend existing `logChange` / reason codes with objective + approved-by fields | Q13 | S | Nearly free; feeds 1.1 |
| 1.4 | **Purpose-per-visit + Value Strip + Planning Evidence panel** (mock data) | Q8 | M | The brief's stated purpose; demo centerpiece. Evidence chain, never attribution claims |
| 1.5 | Keyboard search (`/` or Cmd+K) + scroll-to-block on result | Q1 | S | Cheap win |
| 1.6 | Impact count on "Kalıcı yap" ("138 gelecek ziyaret etkilenir") | Q2 | S | Cheap win |
| 1.7 | Business-event-aware override windows + custom date range ("Mağaza açılana kadar", "Ahmet'in izni boyunca") | Q2 | M | Standout Q2 idea; needs end-date on yama (already mandatory per our model) |
| 1.8 | **Rule Inspector**: per-visit provenance (each task + source + disabled-by + arithmetic 35+8−5=38) | Q7 | S–M | Upgrade of existing `resolveTaskMin` source labels |
| 1.9 | Aggregate Impact Preview on rule change ("147 mağaza · 642 ziyaret · +86s/hafta") | Q7 | M | Generalizes 1.6 |
| 1.10 | Travel connectors from Euclidean x,y (🚗 12dk line between visits); impossible-travel = error under 1.1 (justify to publish) | Q4 | M | No maps API needed; qualitative confidence only |
| 1.11 | **Schedule Repair mocked shell**: trigger banner → 3 canned plans → git-diff review → per-change accept into draft. Qualitative confidence (no %) | Q3 | M | Sells the concept; solver itself is real-build |

## Bucket 2 — Real build only (capture in design doc §5/§9, don't prototype)

| # | Change | Source | Why deferred |
|---|--------|--------|--------------|
| 2.1 | Repair constraint solver + Coverage Decision / Sacrifice Board | Q3, Q10 | Real optimization engine; mock the UX later if a demo needs it |
| 2.2 | Publish communication loop: timing choice, message preview, delivered/read/ack/"Anladım–Sorunum var" tracking, escalation, publish versioning | Q6 | Needs mobile side + backend; planner-side drawer is mockable if demo requires |
| 2.3 | Draft as versioned OCC workspace (per-user drafts, field-level merge) | Q11 | Multi-planner backend; prototype is single-user. Note: needs field-level change tracking in data model |
| 2.4 | Setup Mode / "Generate First Draft" guided cold start | Q9 | Adopt the **two-engine framing** (Planning creates / Operations maintains) in the doc now — free; the generator is real work. Must reconcile "monthly regeneration" vs standing-Baz projection |
| 2.5 | Policy-governance layer (ownership/approval above the rule cascade) | Q13 | Organizational feature; design-doc section only |
| 2.6 | Skills, leave, KPI fields on employees; store business priority (★ skippable → must-visit) | Q3/data model | Add to entity model doc; feeds 2.1 |
| 2.7 | Virtualized rendering at real scale | Q1 | Perf; irrelevant at prototype data size |

## Bucket 3 — Skip / v2+

| # | Item | Source | Why |
|---|------|--------|-----|
| 3.1 | Full-day/route optimization ("Optimize Thursday") | Q4 | GPT itself says V2, opt-in only |
| 3.2 | Confidence percentages on AI plans | Q3 | False precision — violates Q10's own principle; qualitative everywhere |
| 3.3 | Hard blocks with no escape hatch | Q5 | Superseded by 1.1 override-with-reason |
| 3.4 | Gamified fairness score, heatmap dashboards, live collab, offline, NL campaign builder | misc | Low value or moonshot |

## Bucket 4 — Process (not software)

| # | Item | Source |
|---|------|--------|
| 4.1 | Pilot discovery: observe real planners a few days; validate the timeline assumption ("what screen is open 80% of the day?") before feature investment. We're hedged (map/schedule/table views) but validate anyway | Q13 |
| 4.2 | Adopt as explicit product principle: *"Surface the tradeoff/evidence, make every decision defensible — never auto-decide"* — add to design doc §1 | through-line |
