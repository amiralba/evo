# API Contracts

<!-- Owned by: architect. Contract-first: the OpenAPI document generated from the backend
     is the source of truth; TS clients are GENERATED. This file records conventions +
     endpoint inventory. Representative endpoint sketch: design doc §9. -->

## Conventions
- Base URL: `/api/v1`
- Auth: JWT bearer access token (60 min, in the `Authorization` header) + rotating httpOnly
  refresh cookie (14 days, path-scoped to `/api/v1/auth`) via ASP.NET Identity — decided in spec
  002, see `docs/AUTH.md` for the full token model. Two roles — `Supervisor` (full), `FieldAgent`
  (read-only + notes, seeder-only — no account-creation API).
- Error format: unified `application/problem+json` shape (spec 003, implemented — spec 002's auth
  and users endpoints are retrofitted onto it). `code` is the stable English key
  a client switches on; `title`/`detail` are English/developer-facing; `userTitle`/`userMessage`
  are Turkish, curated per `code` in `Evo.Domain.Errors.UserErrorMessages` (an in-code catalog,
  not a DB table — error text is part of the API contract, deploy-reviewed like any other code
  change) — the panel displays these directly and maintains no translation map of its own. No
  RFC 7807 `instance` field. `errors` is present only on validation failures (422 domain-rule
  violations via `EvoValidationException`, or 400 model-binding failures — same `code`, different
  status). Unhandled exceptions never leak details outside `Development`.
```json
{
  "type": "...", "title": "...", "status": 422, "detail": "...",
  "code": "validation_error", "userTitle": "Geçersiz bilgi", "userMessage": "Girdiğiniz bilgilerde bir sorun var...",
  "errors": { "field": ["msg"] }, "traceId": "..."
}
```
- Validation: same rule set runs live in UI and enforced at write (design §3.2). Validation failures that are overridable return the violation list; publishing past 🔴 errors requires `justification` + actor in the request (design v0.5 publish gate).
- Versioning: URL segment v1; breaking changes need a flagged decision
- Mutations that affect schedules NEVER apply directly — they stage into draft; only `POST /publish` applies (design §10 "Publish gate confirmed")

## Endpoint inventory (from design §9 — implement per module spec)
| Area | Endpoints | Spec |
|---|---|---|
| Auth | `POST /auth/login`, `POST /auth/refresh`, `POST /auth/logout`, `GET /auth/me`, `POST /auth/change-password` | 002 |
| Users | `POST /users` (Supervisor only), `GET /users`, `GET /users/{id}`, `PATCH /users/{id}`, `POST /users/{id}/activate`, `POST /users/{id}/deactivate` (no delete) | 002 |
| Audit | `GET /audit-log?entityType=&page=&pageSize=` (Supervisor only, paged, newest-first) | 003 |
| Stores | `POST /stores/sync` (Supervisor only, audit-logged, on-demand — also runs nightly via a BackgroundService), `GET /stores?province=&district=&active=&format=&page=&pageSize=` (paged), `GET /stores/{id}` (with revenue + flags). **2026-07-20 (4-layer model, L1):** `PATCH /stores/{id}/status` body `{"active": bool}` — flips `Store.Active` and regenerates every route holding the store; deactivating a store KEEPS its `RouteStop` membership open (no `EffectiveTo`) but `PlanGenerationService` now skips stops whose store is inactive, so the store simply drops out of the materialized plan without severing route membership; writes an `audit_log` entry `entityType=Store`. | 004 |
| Stores/map | **LANDED (spec 006).** `GET /stores/geo?province=&district=&onRoute=` — any authenticated user; bulk `StoreGeoDto[]` (lat/lng, chain/category/format, active-route id+code if any, 6-month revenue), required `province`, capped at 5000. Feeds the planner map's store layer. Deferred: bbox/polygon spatial query (client-side turf point-in-polygon lasso instead), `GET /stores/{id}/summary`. `GET /stores/{id}/task-plan` landed spec 008 (see Tasks/rules row). | 006 |
| Routes | `POST /routes` (Supervisor only), `GET /routes?province=&status=&page=&pageSize=` (paged), `GET /routes/{id}`, `PATCH /routes/{id}` (rename/target/activate/deactivate — Draft→Active requires an active Assignment else 409; Active→Inactive releases stops + drops future visits; Inactive→Active returns empty) | 005 |
| Route stops | `POST /routes/{id}/stops:bulk` (V3/V4 reject with reason, accepted get regenerated), `PATCH /routes/{id}/stops/{sid}` (`serviceMinutes` **snapped to nearest 5, clamped [10,240] — spec 007**; **2026-07-20 (4-layer model, L4)**: body also accepts `weekdayMask` (int16) — with `frequency=Weekly` + a mask set, the stop is visited only on the chosen weekdays, bit0=Mon…bit4=Fri, e.g. Mon/Wed/Fri = `0b10101` = 21; writes `RouteChangeEvent.FreqChanged` when either field changes), `POST /routes/{id}/stops/{sid}:move` (atomic, regenerates both routes), `POST /routes/{id}/stops:reorder` (**LANDED, spec 006** — Supervisor only, `ReorderStopsRequest{stopIds}` must exactly match the route's active stops or 422, reassigns `sequence` in one transaction + one audit-log entry, returns `RouteDetailDto`; backs the panel's drag-and-drop stop reorder in a single call instead of N per-stop PATCHes), `DELETE /routes/{id}/stops/{stopId}` (**2026-07-20, 4-layer model, L3** — "remove from route → pool"; soft-closes the `RouteStop` by setting `EffectiveTo = today`, per the no-delete rule; 204 No Content; writes `RouteChangeEvent.StopRemoved`; regenerates the route's future horizon) | 005 / 006 / 007 |
| Assignment | `POST /routes/{id}/assignment` (reason REQUIRED, 422 if missing; 409 if the merchandiser already holds an active assignment elsewhere — DB constraint) | 005 |
| Patches | `POST /routes/{id}/patches` (`endsOn` REQUIRED — V9, 422 if missing). **Spec 007**: `TimeShift` (type 5) requires `storeId` + `paramsJson={"startMinutes":<int>}` (minutes since midnight) — 422 without; `PatchResolver` pins the store's visit start (never earlier than the day's cursor) and `DayScheduler` reflows downstream visits. `MoveVisit` (type 6, new) requires `storeId` + `paramsJson={"fromDate":"yyyy-MM-dd","toDate":"yyyy-MM-dd","startMinutes"?:<int>}`, `fromDate != toDate` (422 otherwise) — resolved as skip-on-fromDate + add-on-toDate off a single patch row (one expiry, one audit entry, auto-reverts together). Both types 422 on missing/malformed `paramsJson`. | 005 / 007 |
| Plan/health/validate | `GET /routes/{id}/plan?from=&to=` (materialized visits + per-day findings — `PlannedVisitDto.routeStopId` added **spec 007**; **spec 009** added `status`, `checkInAt`, `checkOutAt`, `actualMinutes`, `outcomeReason`, and `checkInLocation` (nearest `merchandiser_location_ping` to `checkInAt`, null if none within ±30 min or visit not checked in) — future visits return these as null/`Planned`), `GET /routes/{id}/health` (revenue/weekday-minutes/category-mix/finding counts), `POST /routes/{id}/validate` (live findings, no persistence) | 005 / 007 / 009 |
| Publish | `POST /routes/{id}/publish` (runs the validator; Error findings require `reason`+`objective` to override — 422 without, journaled to `decision_journal` with; materializes the horizon). **Spec 009**: after a successful publish, also fires `INotificationDispatcher.DispatchPublishAsync` — writes one mocked `notification` row per currently-assigned merchandiser (log-and-continue on dispatcher failure, never blocks the publish response). | 005 / 009 |
| Simulation | `POST /simulate/route` (what-if: stores[] → revenue+minutes) — still deferred (not in 006's MVP cut) | later M1 |
| Tasks/rules | **LANDED (spec 008).** `GET /task-templates` (active catalog). `GET /stores/{id}/task-plan?date=` — resolved tasks for a store/date via `TaskResolver`: `{storeId, date, visitTotalMinutes, tasks:[{templateId, code, name, minutes, trace:[{layer, op, before, after}], taskInstanceId, status, resultJson}]}` (`taskInstanceId` null if no materialized `TaskInstance` exists for that date yet; **spec 009** added `status`/`resultJson` — the latter a typed `TaskResult` JSON shape per `ProofRequired`, see Field execution row). `GET /rules` (list), `POST /rules` (Supervisor only; body `{taskTemplateId?, scope, condition:{chainId?, format?, category?, channel?, province?, routeId?, storeId?}, effect:{op, setValue?, scaleValue?}, priority, effectiveFrom, effectiveTo?}`; writes an `audit_log` entry `entityType=Rule`; regenerates affected routes' horizon inline before responding). `GET /rules/impact?scope=&taskTemplateId=&chainId=&format=&routeId=&storeId=&op=&setValue=&scaleValue=` — aggregate preview `{stores, visitsPerWeek, deltaMinutesPerWeek, daysOver450}` for a *candidate* (not-yet-created) rule; never persists. `PATCH /task-instances/{id}` body `{minutes, scope}` where `scope` is the literal string `"INSTANCE"` (writes `TaskInstance.OverrideMinutes` for that row only, recomputes that visit's `PlannedEnd`), `"STORE_RULE"`, or `"FORMAT_RULE"` (both create a new store/format-scoped `Rule` from the edit context and regenerate affected routes). `POST /tasks/adhoc` body `{templateCode, name, minutes, targetChain?, targetFormat?, deadline}` — creates a `recurrence=Once` `TaskTemplate` with `validUntil=deadline`, returns `{taskTemplateId, matchingStoreCount}`; regeneration attaches one `TaskInstance` to each matching store's next visit before the deadline. Never-block: a rule edit that pushes a day over 450 min surfaces the existing V2 finding as `Warning` on `GET /routes/{id}/plan`, never blocks the write. Deferred: `GET /tasks/overdue`, standalone Yönetim admin pages (template/rule CRUD UI — `POST /rules` covers creation, no list/edit page yet). | 008 |
| Notes | **LANDED (spec 009).** `GET /notes?status=&kind=&anchorType=` (Supervisor only, newest-first; `NoteDto{id, authorId, authorName, anchorType, anchorId, anchorLabel, kind, body, status, createdAt}` — `anchorLabel` best-effort resolved from the store/visit/day). `PATCH /notes/{id}` body `{status}` — legal transitions `Open→Acknowledged`, `Open→Resolved`, `Acknowledged→Resolved` only (any other transition, e.g. `Resolved→Open`, is 422); writes an `audit_log` entry `entityType=Note`. No create-note endpoint — notes are seeder-produced only in M3 (the field agent's write channel is mobile, deferred). | 009 |
| Field execution | **LANDED (spec 009).** `GET /merchandisers/{id}/location-history?from=&to=&page=&pageSize=` — paged `merchandiser_location_ping` rows newest-first; scoping mirrors `/day` (Supervisor any, FieldAgent self only, else 403). `GET /merchandisers/{id}/notifications` — that agent's mocked notification receipts (`NotificationDto{id, payloadJson, createdAt, readAt}`), same scoping. Both feed the "everything downstream of the field works against seeded data" M3 goal — no live agent API exists; all data is seeded by `FieldExecutionSeederModule`. | 009 |
| Mobile | `GET /merchandisers/{id}/day?date=` — landed early in 005 (Supervisor: any merchandiser; FieldAgent: self only, else 403) since the route lifecycle needed a merchandiser-scoped read; **spec 009** landed the downstream-of-field surface (notes/notifications/location-history) against seeded data — a real mobile app remains out of scope, revived from backlog later | 005 (early) / 009 |
| Absences | **LANDED (spec 010).** `POST /merchandisers/{id}/absences` (Supervisor only; body `{startDate, endDate, reason, note?}`, 422 if `endDate < startDate`; writes an `audit_log` entry `entityType=Absence`), `GET /merchandisers/{id}/absences` (newest-first). Feeds V14/Onarım. | 010 |
| Analytics | **LANDED (spec 010).** All on-read aggregation, no materialized tables (see `docs/DATABASE.md`), all Supervisor-scoped. `GET /analytics/plan-health?region=&from=&to=` → `PlanHealthReportDto{region, from, to, routes:[{routeId, routeCode, routeName, province, completionPct, plannedMinutes, realizedMinutes, durationVariancePct, utilizationPct, utilizationBand, taskCompliancePct, patchLoad:{type:count}, stabilityScore, assignmentTurnover, overrideRatePct, planHealthScore}]}`, routes ranked by `planHealthScore` desc — all 8 design §8 metrics, including `overrideRatePct` and mobility (below). `GET /analytics/stability?region=` → per-route `{routeId, routeCode, stabilityScore}`. `GET /analytics/mobility?region=&months=` (default 12) → `MerchandiserMobilityDto[]{merchandiserId, name, distinctRoutesHeld, intraRouteReshuffles, regionalMedianRoutesHeld, outlier}`. `GET /routes/{id}/evidence?weeks=` (default 4) → `RouteEvidenceDto{routeId, weeks, stores:[{storeId, storeName, planned, done, missed, skipped, durationVariancePct}], causalityDisclaimer:true}` — always renders "kanıt, nedensellik değil". **Divergence from design §8:** `mobility`/`overrideRatePct` are shipped Supervisor-scoped, NOT gated behind a "senior management" role — EVO's 2-role model has no such tier; framed as outlier-surfacing, not punitive (explicit user override of the planner's narrower recommendation — see `docs/DECISIONS.md`, 2026-07-18). | 010 |
| Onarım | **LANDED (spec 010).** Absence-repair decision workbench (design §7.3b) — ranks-not-decides, never auto-plans. All Supervisor only. `GET /onarim/disruptions?region=` → `DisruptionDto[]{id, kind (Absence\|StoreClosure), label, start, end, affectedVisitCount}`. `GET /onarim/disruptions/{id}/affected-visits` → `AffectedVisitDto[]{plannedVisitId, routeId, routeCode, storeId, storeName, date, startMinutes, plannedMinutes, candidates:CandidateDto[]{merchandiserId, name, routeId?, available, capacityMinutesAfterMove, withinCapacity, regionProximity, reasoning, rank}}` — ranked via pure `Evo.Domain.Onarim.CandidateRanker` (available+within-capacity first, then same-province, then distance, then spare capacity; deterministic tie-break). `POST /onarim/disruptions/{id}/apply` — body `{reason, objective, decisions:[{plannedVisitId, action, targetDate?, targetMerchandiserId?, targetRouteId?}]}` where `action` ∈ `Skip\|MoveDay\|ReassignRoute\|ReassignPerson` (422 without `reason`+`objective` — override-with-reason gate); maps to existing patch types (`SkipStore`/`MoveVisit`/`ReassignTemp`) plus a **new `ReassignPerson` action backed by a new `CrossReassignVisit` PatchType** (v1 addition beyond the original 3 — pairs a skip-effect on the source route with an add-effect on a different target route off ONE patch row, structurally analogous to how `MoveVisit` pairs skip/add across two DATES on one route, but crosses two ROUTES on one date — see `docs/DECISIONS.md`, 2026-07-18); regenerates all affected route(s) and writes one `decision_journal` (`Kind=OnarimRepair`) entry. | 010 |

## Client generation
`npm run generate-api-client` (panel + mobile) regenerates from the OpenAPI doc; run after ANY backend contract change; generated code is never edited by hand. Pipeline defined in spec 001.
