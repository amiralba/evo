---
description: Plan a feature — clarify, spec, and fine-grained task breakdown in specs/NNN-slug/.
---

Use the planner subagent to plan: $ARGUMENTS

Process: read CLAUDE.md and existing specs/ → clarify with numbered questions (record answers in the spec) → create specs/NNN-slug/ from specs/_template/ → break into 2–5 minute tasks with exact file paths and verification steps. Report the plan to me before any implementation starts.
